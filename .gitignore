# Dependências (nunca devem ir para o Git)
node_modules/

# Arquivos de Ambiente e Segredos (IMPORTANTE!)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Certificados de Segurança (IMPORTANTE!)
*.key
*.cert
*.pem

# Logs de erro e acesso (que configuramos com o Winston)
*.log
access.log
error.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Arquivos de build do React (opcional, mas recomendado)
frontend/build/
backend/dist/

# Arquivos de sistema
.DS_Store
Thumbs.db